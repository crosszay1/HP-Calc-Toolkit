[project]
name = "hp-calc-toolkit"
version = "0.1.0"
description = "A set of tools for develoing on the HP prime G2 graphing caluclator."
readme = "README.md"
requires-python = ">=3.14"
dependencies = []

[dependency-groups]
dev = [
    "ruff>=0.16.5",
]
